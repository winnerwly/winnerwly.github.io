# 项目规范

## 项目结构与模块组织

本仓库是 Hexo 静态博客项目。站点配置位于 `_config.yml`，依赖与脚本位于 `package.json`。博客文章放在 `source/_posts/`，文章相关图片或示例文件可放在同名资源目录中。独立页面位于 `source/about/`、`source/category/`、`source/tag/`、`source/friends/`、`source/ins/`。当前主题为 `themes/chic/`，其中 EJS 模板位于 `layout/`，Stylus 样式位于 `source/css/`，脚本位于 `source/js/`，图片与字体资源位于 `source/img/` 和 `source/fonts/`。

## 常用命令

- `npm install`：安装 Hexo 与渲染依赖。
- `npm run start`：清理、生成并启动本地预览服务。
- `npm run build`：清理并生成静态文件到 `public/`。
- `npx hexo new "文章标题"`：基于 `scaffolds/post.md` 创建新文章。

本项目暂无独立自动化测试脚本。内容、主题或配置变更后，优先使用 `npm run build` 作为基础验证。

## 编码与命名规范

文章使用 Markdown 与 YAML Front Matter，常用字段包括 `title`、`tags`、`category`、`date`。现有文章文件名多为时间戳形式，例如 `2604201000.md`，手动新增文章时应保持一致。YAML、EJS 嵌套结构建议使用两个空格缩进。主题改动应保持边界清晰：结构改 EJS，视觉改 `.styl`，浏览器行为改 `themes/chic/source/js/`。

## 项目开发流程要求

### 1. 核心原则

所有需求、新功能、问题修复、逻辑调整、页面改造、接口改动、配置变更、架构优化等开发事项，必须遵循：

**先分析、再文档、经确认、后开发、再总结。**

未经确认，不允许直接进入开发阶段。

### 2. 文档语言要求

项目内所有开发相关文档必须统一使用中文编写，包括但不限于：

- 需求分析文档；
- 技术方案文档；
- 变更说明文档；
- 开发总结文档；
- 测试说明文档；
- 接口说明文档；
- 架构设计文档；
- AI 工具生成的开发文档。

除专有名词、代码字段、接口字段、命令、配置项、文件路径、变量名、函数名等技术内容外，不允许整篇文档使用英文编写。

### 3. 需求分析要求

在开始任何开发工作前，必须先进行深度需求分析，至少明确：

- 需求背景：为什么要做、当前问题是什么、期望解决什么痛点。
- 需求目标：最终效果是什么、本次包含什么、本次不包含什么。
- 影响范围：涉及哪些页面、组件、接口、路由、权限、配置、数据结构，是否影响已有功能。
- 技术方案：采用什么实现方式，关键逻辑如何设计，是否需要新增公共能力，是否存在替代方案。
- 风险点：潜在问题、线上影响、回滚方案、额外测试重点。

### 4. 开发文档要求

所有需求和变更在开发前，必须先编写中文开发文档。开发文档必须统一存放在项目根目录下的 `docs` 目录中，不允许散落在其他目录。

推荐目录结构：

```bash
docs/
  requirements/
    2026-07-05-xxx-feature.md
  changes/
    2026-07-05-xxx-change.md
  reviews/
    2026-07-05-xxx-summary.md
```

文档命名建议使用 `YYYY-MM-DD-需求名称.md`。

### 5. 开发文档内容规范

每一份开发文档至少包含：

- 需求名称；
- 一、需求背景；
- 二、需求目标；
- 三、需求范围；
- 四、现状分析；
- 五、技术方案；
- 六、影响范围；
- 七、风险评估；
- 八、测试重点；
- 九、确认记录。

确认记录必须包含确认人、确认时间、确认结论。需求文档未确认前，不允许进入开发。

### 6. 确认机制与快速通道

本流程分两档：

**需求变更**（新功能、页面改造、接口/配置变更、逻辑重构等）必须走完整确认流程：开发文档编写完成后，必须提交给需求负责人或项目负责人确认。只有在明确收到确认后，才能进入开发阶段。

确认前禁止：

- 直接修改业务代码；
- 直接调整接口逻辑；
- 直接改动公共组件；
- 直接修改配置文件；
- 直接重构代码；
- 直接提交开发结果。

**缺陷修复及简单小需求**可走快速通道：无需事前编写开发文档、无需事前确认，可直接处理；但完成后必须在 `docs/`（建议放 `docs/changes/`）记录变更内容、影响范围、验证结果。若修复过程中发现范围扩大、涉及公共组件或基础架构，必须升级为需求变更流程，补开发文档并确认后再继续。

如果需求在开发过程中发生变化，必须重新更新开发文档，并再次确认后才能继续开发。

### 7. 开发过程要求

开发过程中必须严格按照已确认的开发文档执行。如发现以下情况，必须暂停开发并重新确认：

- 实际实现与文档方案不一致；
- 需求范围发生变化；
- 接口结构发生变化；
- 页面交互发生变化；
- 权限逻辑发生变化；
- 数据结构发生变化；
- 出现新的风险点；
- 需要额外改动公共能力或基础架构。

不得擅自扩大需求范围，不得在未确认的情况下增加额外功能。

### 8. 开发完成后的总结要求

开发完成后，必须回顾原开发文档，并输出中文开发总结。总结文档统一存放在 `docs/reviews/`。

总结文档需要包含：

- 一、本次完成内容；
- 二、与原开发文档的对照；
- 三、影响范围回顾；
- 四、测试情况；
- 五、遗留问题；
- 六、经验总结。

如实际开发内容与原文档存在差异，必须说明差异点、产生原因、是否已确认、最终处理方式。

### 9. 强制执行要求

本规则为项目开发的强制流程要求。任何人或任何 AI 编码工具在参与本项目开发时，都必须严格遵守：

1. 先进行需求深度分析；
2. 再编写中文开发文档；
3. 文档统一存放到 `docs` 目录；
4. 等待负责人确认；
5. 确认后再进入开发；
6. 开发完成后回顾原开发文档；
7. 输出中文开发总结；
8. 总结文档继续存放到 `docs` 目录。

未经过文档确认的需求，不允许进入开发阶段。未使用中文编写的开发文档，不视为有效文档。未输出开发总结的需求，不视为完整交付。

缺陷修复及简单小需求适用第 6 节的快速通道：可跳过事前文档与确认直接处理，但事后必须在 `docs/` 记录变更内容、影响范围与验证结果；若范围扩大或涉及公共组件与基础架构，须升级为需求变更流程。

## 提交与 Pull Request 要求

提交信息应尽量清晰，例如 `feat: add AI development posts` 或 `fix: correct theme header layout`。Pull Request 应说明变更内容、验证结果，涉及界面改动时应附截图。除非部署流程明确需要，不要提交 `public/` 等生成产物。

## 部署与配置提示

GitHub Pages 部署配置位于 `.github/workflows/pages.yml`。不要提交密钥、私有令牌或机器专属路径。修改站点元信息、URL、分页、渲染行为时，应同步更新 `_config.yml` 并验证生成结果。
